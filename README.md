Docker Setup Command : **docker compose up -d --build**

Pocketbase Port :8090

Frontend Port :5173

Backend Port :8000

Create a .env file at root with these variables
- PB_ADMIN_EMAIL
- PB_ADMIN_PASSWORD